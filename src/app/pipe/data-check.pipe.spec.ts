import { DataCheckPipe } from './data-check.pipe';

describe('DataCheckPipe', () => {
  it('create an instance', () => {
    const pipe = new DataCheckPipe();
    expect(pipe).toBeTruthy();
  });
});
